PORT=3300
# MongoDB connection string (replace with your cluster / local URI)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/devdose
# Optional secret values used by auth (set a secure value in production)
JWT_SECRET=your_secret_here